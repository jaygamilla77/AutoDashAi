<article class="public-page">
  <header class="lead-hero">
    <h1>AutoDash AI Features</h1>
    <p>
      Explore the capabilities that make AutoDash AI a leading AI dashboard builder and KPI
      dashboard software for modern, data-driven teams.
    </p>
  </header>

  <section aria-labelledby="ai-gen-heading">
    <h2 id="ai-gen-heading">AI dashboard generation</h2>
    <p>
      Connect any data source and AutoDash AI assembles a complete, interactive dashboard
      automatically. Our AI analytics platform handles KPI discovery, chart selection, layout,
      and styling — eliminating hours of manual configuration.
    </p>
  </section>

  <section aria-labelledby="kpi-heading">
    <h2 id="kpi-heading">KPI recommendations</h2>
    <p>
      The platform analyzes your dataset and recommends the most relevant key performance
      indicators based on data shape, semantics, and business context. Each KPI card is
      enriched with trend indicators, period comparisons, and AI-generated commentary.
    </p>
  </section>

  <section aria-labelledby="charts-heading">
    <h2 id="charts-heading">Smart chart suggestions</h2>
    <p>
      AutoDash AI understands when to use a bar, line, pie, gauge, heatmap, scatter, or table —
      and applies the most effective option automatically. Charts are always optimized for
      readability and decision making.
    </p>
  </section>

  <section aria-labelledby="sources-heading">
    <h2 id="sources-heading">Multi-source data integration</h2>
    <p>
      Connect SQL databases, upload Excel and CSV files, ingest JSON, or call REST APIs.
      Combine operational, financial, HR, and sales data into unified executive dashboards
      with a single, consistent analytics layer.
    </p>
  </section>

  <section aria-labelledby="interactive-heading">
    <h2 id="interactive-heading">Interactive dashboards</h2>
    <p>
      Filter by date range, segment, or dimension. Drill down into any chart. Reorder panels
      with drag-and-drop. AutoDash AI delivers fully interactive dashboards that respond to
      every question your team asks of the data.
    </p>
  </section>

  <section aria-labelledby="insights-heading">
    <h2 id="insights-heading">AI insights &amp; executive summaries</h2>
    <p>
      Every dashboard includes optional AI insights — plain-language explanations of trends,
      anomalies, and outliers. Executives can read a dashboard the same way they read a
      written report, while still drilling into the underlying numbers.
    </p>
  </section>

  <section aria-labelledby="sharing-heading">
    <h2 id="sharing-heading">Secure sharing &amp; collaboration</h2>
    <p>
      Share dashboards via secure, token-based public links. Add password protection,
      expiration dates, or read-only access for stakeholders. AutoDash AI is built for
      collaborative reporting across teams and organizations.
    </p>
  </section>

  <section aria-labelledby="realtime-heading">
    <h2 id="realtime-heading">Real-time analytics</h2>
    <p>
      Live database connections power real-time analytics dashboards. Refresh KPIs on demand,
      schedule automated updates, and keep operations teams aligned with the latest numbers.
    </p>
  </section>
</article>
